@model dynamic
@{
    ViewBag.Title = "Reservar Película";
}

@*<h2>Reservar Entradas</h2>

@using (Html.BeginForm("ReservarPelicula", "Reserva", FormMethod.Post))
{
    <div class="form-group">
        <label for="cantidadEntradas">Cantidad de Entradas</label>
        <input type="number" id="cantidadEntradas" name="cantidadEntradas" class="form-control" min="1" onchange="actualizarPrecio()" required />
    </div>

    <div class="form-group">
        <label for="salaSeleccionada">Sala</label>
        @Html.DropDownList("salaSeleccionada", (IEnumerable<SelectListItem>)ViewBag.Salas, new { @class = "form-control", @onchange = "actualizarPrecio()" })
    </div>

    <div class="form-group">
        <p id="totalPrecio">Total: ₡0.00</p>
    </div>

    <button type="submit" class="btn btn-primary">Finalizar Pago</button>
}

<script>
    function actualizarPrecio() {
        var cantidad = parseInt(document.getElementById("cantidadEntradas").value) || 0;
        var precioSala = parseInt(document.getElementById("salaSeleccionada").value) || 0;
        var total = cantidad * precioSala;
        document.getElementById("totalPrecio").innerText = "Total: ₡" + total.toFixed(2);
    }
</script>*@
